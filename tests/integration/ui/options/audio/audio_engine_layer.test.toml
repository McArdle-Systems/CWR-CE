binary = "PoseidonGame"
no_menu = true
timeout = 60
# The EAX round-trip requires OpenAL Soft's EFX extension; CI's macOS null
# audio device does not advertise it.
tags = ["headless", "efx-required"]
